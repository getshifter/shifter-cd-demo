"use strict";

const axios = require("axios");
const { Auth, Sites } = require("@shifter/node");
const client = new Auth();

module.exports.login = async (event, context) => {
  console.log(event);
  login;
};